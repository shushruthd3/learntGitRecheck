package com.example.myapplication;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        //Commiting on loacl and then raising pull request to merge to main branch
        //so lets do some changes and push the changes to the main branch later
        //So wel come to the world of learning the Git thing and pushing the changes.
        //how do you feel learning the git and then working on it.
        // Please do share the feedbacks so that we can improvise on the areas.
        setContentView(R.layout.activity_main);
    }
}